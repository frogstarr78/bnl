require 'spec_helper'
require 'fixtures'

module Bnl
  describe "Dsl::ActionItem" do
    before :each do 
      @parser = Bnl::DslParser.new
    end

    it "has to_s"
    it "executes correctly"
  end
end
